Functional - feed that displays a list of items, feed items can have comments.
We are only concerned with user post type, e.g. text/image etc
Should support infinite scroll

Non-functional - should be mobile friendly, does not need to work offline but nice if we can access some data offline. Should be a11y friendly. Should support internalisation. Should be performant. Should have observability, e.g. datadog.

